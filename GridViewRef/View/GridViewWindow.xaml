<Window x:Class="GridView.View.GridViewComponentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GridView.View"
        mc:Ignorable="d"
        Title="Grid View Component" Height="450" Width="800"
        SizeToContent="WidthAndHeight"
        MinHeight="300"
        >
    <Grid x:Name="TopContainer">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Coral" BorderThickness="1" CornerRadius="10" 
                Grid.Row="0" 
                >
            <Label VerticalAlignment="Stretch" HorizontalAlignment="Center"
               Content="{Binding Title}"
               FontSize="25"               
               />
        </Border>
        <ListView x:Name="WrapingListView" Grid.Row="1">
            <ListView.View>
                <GridView>
                    
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
